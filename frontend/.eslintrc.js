module.exports = {
    extends: ['plugin:vitest/recommended'],
    rules: {
        // Your custom rules
    },
    env: {
        'vitest-globals': true, // Recognize Vitest globals like `describe`, `it`, `expect`
    },
};
